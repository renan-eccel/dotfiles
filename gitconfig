[core]
    pager = delta

[delta]
    syntax-theme = Dracula
    line-numbers = true
[user]
	name = renan-eccel
	email = renan.eccel@mercadolivre.com
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
